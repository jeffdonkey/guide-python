# tuple
# a tuple is a variable that stores multiple items
# duplicate item are ALLOWED in a tuple
# tuples are UNCHANGEABLE: items cannot be added or removed from a tuple

my_tuple = ('fish', 'bird', 'chicken')

# tuples are indexed like arrays

print(my_tuple[1])
    # the above should show 'bird' in the terminal

